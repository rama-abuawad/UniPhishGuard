// Local development uses the page origin. Production deployments may replace
// this value with the public HTTPS API origin without changing taskpane.js.
window.UNIPHISHGUARD_API_BASE_URL = window.UNIPHISHGUARD_API_BASE_URL || "";
