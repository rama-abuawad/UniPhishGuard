# Committee demo messages

Run `python generate_demo_assets.py` from `backend/` to generate the deterministic QR image, suspicious ZIP, and scenario list. The files contain documentation-only test content and no confidential or live university email.

Use the scenario list to create messages in a controlled test mailbox, attach the generated files where specified, and scan them through the normal Outlook add-in.
